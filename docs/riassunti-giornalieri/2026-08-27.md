# 2026-08-27 — Licensing, primo passo fuori da CRUDBooster, hardening Settings

- Corretto un bug di licensing: veniva riusato un access token stantio letto
  dal file di licenza invece di richiederne uno nuovo legato al cookie di
  sessione custom. Approfittato per un refactoring di pulizia in
  `ConnectorService`, con fix di un crash quando il server di licenza remoto
  è irraggiungibile e nuovi test di caratterizzazione
  (`docs/refactoring/005-connectorservice-cleanup.md`).
- Avviato il percorso di uscita da `packages/crocodicstudio/crudbooster`:
  spostati i 21 controller "schermata" (di sistema) in
  `App\Http\Controllers\System`, primo tassello di un piano più ampio
  completato il giorno dopo (`docs/refactoring/006-*.md`).
- Upload file: salvato il path relativo invece dell'URL assoluto, rimosso un
  controllo HTTP che rendeva fragile la verifica "file rotto"
  (`docs/refactoring/007-upload-path-relativo.md`).
- Batch di bug fix minori: creare un nuovo privilegio non deve più cambiare
  il tema di chi lo crea, log utente non più modificabili, crash sul
  dettaglio privilegio, seeder `module_helpers` aggiornato (modulo "Module
  Helpers" mancava una riga in `cms_moduls`, stesso 404 poi rivisto anche per
  "Dashboard Layouts" il giorno dopo).
- Risolto un bug sistemico sui 7 componenti "relazione" (popup "Browse
  data"): non si chiudeva dopo la selezione
  (`docs/refactoring/010-popup-select-non-si-chiude.md`); in un secondo
  intervento corretto anche che mostrava pure i record già collegati.
- Aggiunto `docs/ui-ux-annotazioni.md` per raccogliere note in vista del
  futuro rinnovo UI/UX.
- Nascosti i pulsanti dei moduli Qlik/Chat AI quando non coperti da licenza.
- Sezione Settings: hardening (autorizzazione mancante su delete-file,
  upload con path relativo, rimozione gruppo Qlik dai seeder, campo
  password, ecc. — `docs/refactoring/011-settings-hardening.md`), poi in un
  secondo passaggio l'upload fallito non è più silenzioso e mostra la
  preview delle immagini.
- Email Templates: lo slug non è più scrivibile manualmente, si calcola dal
  nome.
