# 2026-08-28 — packages/crudbooster svuotata, Dashboard Layouts, Statistic Builder, tour di bug fix

- Corretta l'icona "Helper" nell'header admin: apriva anche la Chat AI oltre
  alla pagina di aiuto del modulo.
- **Completata l'uscita dai file infrastrutturali di
  `packages/crocodicstudio/crudbooster`**, iniziata il giorno prima con i
  controller "di sistema": spostate in `App\Http\Controllers\System`/
  `App\Helpers`/`App\Http\Middleware` le 5 classi "motore" (`CBController` e
  affini, con shim `class_alias()` per non rompere i controller custom già
  generati nei clienti), i 14 helper, i middleware `CBBackend`/`CBAuthAPI`,
  `routes.php` (ora `routes/crudbooster.php`, caricato da
  `RouteServiceProvider`), il `CRUDBoosterServiceProvider`, la
  documentazione originale del pacchetto (spostata in `docs/crudbooster/`) e
  gli ultimi asset/vendor legacy. Eliminati anche i comandi installer morti
  e file di validazione ridondanti. **`packages/crocodicstudio/crudbooster/src/`
  ora non contiene più logica applicativa propria** (vedi indice completo
  in `docs/refactoring/README.md`, interventi 012-037).
- Chiusa una vulnerabilità di **open redirect** su `CRUDBooster::redirect()`/
  `return_url` (`docs/refactoring/038-*.md`), e corretto un bug "tipo di
  componente non trovato" causato da un path hardcoded dimenticato fuori da
  `packages/` durante lo spostamento delle view (`039-*.md`).
- Giro di UI/UX e bug fix su Tenants/Privileges/Groups: preview immagini
  Logo/Favicon e nuovo tipo di campo "color" riusabile (con swatch anche in
  lista/dettaglio, non solo nel form) per Tenants; form Privileges allineato
  allo standard `crudbooster::default.form`; cancellazione privilegio che
  era bloccata da una regola sbagliata e perdeva il messaggio d'errore nel
  redirect; messaggio di blocco cancellazione gruppo reso più specifico
  (distingue membri e tenant assegnati); corretti tre crash a catena
  legati a un privilegio o tenant orfano/soft-deleted (utente con
  `is_superadmin` su privilegio cancellato, member list di un gruppo che
  spariva per un JOIN troppo stretto, dettaglio di un campo "select" con FK
  orfana).
- Dashboard Layouts: trovato e corretto lo stesso pattern di 404 già visto
  per "Module Helpers" (mancava la riga in `cms_moduls`); poi sostituita la
  modalità di editing del layout con un builder visuale a righe/colonne al
  posto del vecchio editor TinyMCE (stesso formato HTML salvato), rimossa la
  modalità "Avanzato (HTML)" e aggiunta una preview visiva nel dettaglio.
- Statistic Builder — widget "Small Box" e "Table": select ricercabile per
  l'icona e color picker nativo al posto dei 4 colori fissi; mostrato il
  vero errore SQL invece di un generico "ERROR" (un `die()` rompeva
  l'intera risposta AJAX e faceva sparire il widget); corretto un bug di
  selettore jQuery che rendeva invisibile la validazione dei campi
  obbligatori su tutti i widget; bordo tratteggiato visibile per le aree del
  layout in modalità builder; ogni area ora accetta un solo widget; il
  selettore DataTable del widget Table era troppo generico e poteva
  confliggere con altri Table nella stessa dashboard; `getShow()` non
  caricava il layout assegnato alla dashboard e ricadeva sempre sulla
  griglia di default.
- **Nel pomeriggio**, altro giro di bug fix e piccoli miglioramenti UI su
  Statistic Builder/Menu Management/Module Generator, partiti da una serie
  di segnalazioni manuali dell'utente:
  - icona di "Dashboard Layouts" in sidebar nera invece che bianca come le
    altre voci (mancava il filtro CSS applicato ai font-awesome);
  - campo Icon del Module Generator (step 1) e del form Menu Management:
    aggiunta la preview dell'icona vera accanto al nome (prima solo testo),
    con relativo bug di doppia inizializzazione select2 trovato e corretto
    (lo span generato da select2 viene ripreso e reinizializzato dallo
    script generico della pagina, campo che appare vuoto); stesso
    trattamento (swatch) aggiunto al campo Color dei menu;
  - **bug reale**: rinominare una dashboard ne cambiava lo slug, rompendo il
    permalink pubblico e ogni link/voce di menu esistente verso di essa
    (fatal error invece di redirect pulito); trovata anche la causa più
    diretta del link già rotto in sidebar, un bug di precedenza tra
    operatori in `MenusController` che faceva perdere il prefisso
    `statistic_builder/show/` dal path ad ogni modifica di un menu di tipo
    Statistic;
  - widget Chart Area/Line/Bar completamente vuoti nella pagina builder
    (funzionanti solo nella dashboard live): Raphael.js, richiesto da
    Morris.js per disegnare, non veniva caricato dal layout standalone della
    pagina builder;
  - widget "Module Panel": campo "Route" rinominato in "Modulo da mostrare",
    opzioni ora leggibili (nome modulo + azione, es. "Users Management -
    Lista" invece del controller/metodo tecnico) e ordinate alfabeticamente,
    corretto anche un bug preesistente per cui la scelta salvata non
    risultava mai selezionata riaprendo la configurazione.
  - creato anche uno script locale (`local-scripts/`, escluso da git) per
    popolare dati di test realistici, usato per verificare i widget dello
    Statistic Builder durante queste correzioni.
