<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="bootstrap/autoload.php"
         colors="true"
         stopOnFailure="false"
         cacheDirectory=".phpunit.cache">
    <testsuites>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
    </testsuites>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <!-- CACHE_STORE è il nome usato da Laravel 11+; questo progetto tiene
             la propria chiave CACHE_DRIVER in config/cache.php (mai
             rinominata durante gli upgrade), quindi resta quella giusta.
             Senza questa, i test userebbero la cache su file di default,
             condivisa con qualunque altro processo dell'app sullo stesso
             container/filesystem — causa di test non isolati/flaky. -->
        <env name="CACHE_DRIVER" value="array"/>
        <!--<env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="testing2"/>-->
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
